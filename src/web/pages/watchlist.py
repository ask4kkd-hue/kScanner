"""web/pages/watchlist.py — Stub; built out in Part E."""

from nicegui import ui

import components as comp


def render(con, state: dict) -> None:
    comp.page_title("Watchlist")
    ui.label("Part E not yet built.").classes("text-sm")
