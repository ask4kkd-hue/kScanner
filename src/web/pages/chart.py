"""web/pages/chart.py — Stub; built out in Part C."""

from nicegui import ui

import components as comp


def render(con, state: dict) -> None:
    comp.page_title("Chart")
    ui.label("Part C not yet built.").classes("text-sm")
