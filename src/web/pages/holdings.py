"""web/pages/holdings.py — Stub; built out in Part F."""

from nicegui import ui

import components as comp


def render(con, state: dict) -> None:
    comp.page_title("Holdings")
    ui.label("Part F not yet built.").classes("text-sm")
