"""web/pages/performance.py — Stub; built out in Part H."""

from nicegui import ui

import components as comp


def render(con, state: dict) -> None:
    comp.page_title("Performance")
    ui.label("Part H not yet built.").classes("text-sm")
